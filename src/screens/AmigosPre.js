import React from 'react';
import { View, Text, Button } from 'react-native';

const FriendsScreen = ({ navigation }) => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Amigos Pre</Text>
      
    </View>
  );
};

export default FriendsScreen;